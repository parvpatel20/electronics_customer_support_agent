[project]
name = "techcart-ai"
version = "1.0.0"
description = "TechCart AI — production multi-agent electronics customer support backend"
requires-python = ">=3.11,<3.14"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py311"
